<% content_for :page_title, @department.name %>
<% content_for :page_subtitle, "Department details." %>

<div class="bg-white border border-gray-200 p-6 max-w-3xl">
  <dl class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <div>
      <dt class="text-sm font-semibold text-gray-500">Name</dt>
      <dd class="mt-1 text-sm"><%= @department.name %></dd>
    </div>

    <div>
      <dt class="text-sm font-semibold text-gray-500">Status</dt>
      <dd class="mt-1 text-sm"><%= @department.active? ? "Active" : "Inactive" %></dd>
    </div>

    <div class="md:col-span-2">
      <dt class="text-sm font-semibold text-gray-500">Description</dt>
      <dd class="mt-1 text-sm text-gray-700"><%= @department.description.presence || "No description provided." %></dd>
    </div>

    <div>
      <dt class="text-sm font-semibold text-gray-500">Created At</dt>
      <dd class="mt-1 text-sm"><%= @department.created_at.strftime("%d %b %Y, %I:%M %p") %></dd>
    </div>

    <div>
      <dt class="text-sm font-semibold text-gray-500">Last Updated</dt>
      <dd class="mt-1 text-sm"><%= @department.updated_at.strftime("%d %b %Y, %I:%M %p") %></dd>
    </div>
  </dl>

  <div class="mt-6 flex items-center gap-3">
    <%= link_to "Edit", edit_department_path(@department), class: "bg-amber-600 hover:bg-amber-700 text-white px-4 py-2 text-sm font-medium" %>
    <%= link_to "Back", departments_path, class: "border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50" %>
  </div>
</div>